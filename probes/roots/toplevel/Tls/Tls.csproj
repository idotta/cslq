<Project Sdk="Microsoft.NET.Sdk">
  <!-- Never built, never restored: only the shape of the tree is under test. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
</Project>
